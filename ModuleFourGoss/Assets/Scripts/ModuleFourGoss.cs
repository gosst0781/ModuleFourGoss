//Thayer Goss, Assignment: Version Control, 3/3/2026
using UnityEngine;

public class AssignmentQuestionsAndAnswers : MonoBehaviour
{
    private void Start()
    {
        
    }

    private void Update()
    {
        
    }

    /*
     Q1: What are two bennefits of version control discussed in class?
     A: Bennefit one is that IF something were to go wrong, it's possible to backtrack to or get an earlier version of the project, effectively backing up your work. 
        Bennefit two is that it allows you to track what changes are made, and who made said changes via PR history.

     Q2: What are the two main types of version control systems?
     A: The two types of version control that are popularly used are Distributed and Centralized version control systems. 
        Centralized systems use a centralized server to store your file history and controls the access to said files, and kept separately from the working copy.
        Distributed systems are programs that stow the history of your project files in a folder on the hard drive, which acts as your working copy for said progam.

     Q3: What is a conflict? (version control)
     A: Conflicts occur when two users make simultaneous changes to the same program, which create a version conflict which often requires manual intervention and review.

     Q4: What is one way to avoid conflicts when using version control? (as discussed in class)
     A: Conflicts can be avoided by not having multiple people work on the same file or section of code in a file.
     */
}